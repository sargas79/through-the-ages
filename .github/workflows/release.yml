name: Release

on:
  push:
    tags:
      - "v*"
      - "V*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run unit tests
        run: npm test

      - name: Check the manifest
        run: node tools/check-manifest.mjs

      - name: Derive version from tag
        id: version
        run: |
          tag="${GITHUB_REF_NAME}"
          echo "value=${tag#[vV]}" >> "$GITHUB_OUTPUT"

      - name: Set release manifest metadata
        run: |
          node -e "
            const fs = require('fs');
            const manifest = JSON.parse(fs.readFileSync('module.json', 'utf8'));
            manifest.version = '${{ steps.version.outputs.value }}';
            manifest.download = 'https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/module.zip';
            fs.writeFileSync('module.json', JSON.stringify(manifest, null, 2) + '\n');
          "

      - name: Reconcile the changelog with the tag
        run: node .github/scripts/release-changelog.mjs "${{ steps.version.outputs.value }}" release-notes.md

      - name: Build the package
        run: |
          zip -r module.zip \
            module.json LICENSE README.md CHANGELOG.md \
            scripts templates styles lang

      - name: Publish the release
        uses: softprops/action-gh-release@v2
        with:
          name: Through the Ages ${{ github.ref_name }}
          files: |
            module.json
            module.zip
          body_path: release-notes.md
